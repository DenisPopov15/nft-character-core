definitions:
  SessionToken:
    type: object

    properties:
      token:
        type:   string
        description: >-
          User auth session token.

      nfts:
        type: 'array'
        items:
          $ref: '#/definitions/NFTObject'
        description: >-
          User auth session token.
